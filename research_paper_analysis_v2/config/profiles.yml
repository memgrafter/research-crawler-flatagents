spec: flatprofiles
spec_version: "1.0.0"

data:
  model_profiles:
    # Use fully-qualified LiteLLM model ids to avoid provider/name assembly ambiguity.
    prototype_structured:
      # Prep/cheap phases — Trinity has 20 RPM, good enough for high-volume work
      name: openrouter/arcee-ai/trinity-large-preview:free
      temperature: 1.0

    prototype_reasoning:
      # Expensive phases (why, open_questions, reproduction, limits) — Step wins +3.2 avg
      name: openrouter/stepfun/step-3.5-flash:free
      temperature: 1.0

  default: prototype_structured
