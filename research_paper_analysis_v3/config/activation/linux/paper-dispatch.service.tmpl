[Unit]
Description=Paper machine dispatch once

[Service]
Type=oneshot
WorkingDirectory={{WORKDIR}}
ExecStart={{PYTHON}} -m flatmachines.dispatch_signals --once --resumer config-store --db-path {{DB_PATH}}
